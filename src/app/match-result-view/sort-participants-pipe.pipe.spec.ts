import { SortParticipantsPipePipe } from './sort-participants-pipe.pipe';

describe('SortParticipantsPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SortParticipantsPipePipe();
    expect(pipe).toBeTruthy();
  });
});
